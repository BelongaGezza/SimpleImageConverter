# Quick Start — {{PROJECT_NAME}}

1. Install Rust toolchain: `rustup toolchain install stable`
2. Clone repository: `git clone git@github.com:YOUR_ORG/{{PROJECT_NAME}}.git`
3. Build: `cargo build`
4. Run tests: `cargo test`
5. Start development: follow `IMPLEMENTATION_PLAN.md` for sprint structure

Replace `{{PROJECT_NAME}}` and `YOUR_ORG` appropriately.